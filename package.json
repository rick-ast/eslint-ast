{
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "prepare": "husky",
    "lint": "eslint . --fix",
    "release": "node scripts/release.mjs"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "eslint-config-ast": "workspace:^",
    "execa": "^8.0.1",
    "husky": "^9.0.11",
    "lint-staged": "^15.2.2",
    "prettier": "^3.2.5"
  },
  "lint-staged": {
    "*": [
      "prettier --write --cache --ignore-unknown"
    ],
    "*.js": [
      "eslint --fix"
    ]
  }
}

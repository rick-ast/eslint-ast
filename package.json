{
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "prepare": "husky",
    "lint": "eslint . --fix",
    "release": "node scripts/release.mjs"
  },
  "devDependencies": {
    "eslint": "^8.56.0",
    "execa": "^8.0.1",
    "husky": "^9.0.7",
    "lint-staged": "^15.2.0",
    "prettier": "^3.2.4"
  },
  "lint-staged": {
    "*": [
      "prettier --write --cache --ignore-unknown"
    ],
    "*.js": [
      "eslint --fix"
    ]
  }
}
